package <%= package %>

import (
	<%- imports.each do |i| -%>
		"<%= i %>"
	<%- end -%>
)

func <%= func %>(<%= parameters %>) (err error) {
	<%- segments.each do |s| -%>
		<%= s.to_go %>
	<%- end -%>
	return
}